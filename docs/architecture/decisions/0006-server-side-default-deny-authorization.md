# ADR 0006: Server-side default-deny authorization

**Status:** Accepted

Every sensitive server read, mutation and document-link request evaluates active identity, role, target, state and scope. UI is never authorization. Audit candidate/file access, hiring actions, retention, publication and role/account changes.
