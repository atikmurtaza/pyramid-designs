# ADR 0004: Direct private quarantine uploads

**Status:** Accepted

After server validation of intent, browsers upload only through short-lived, narrow permission to private quarantine. Keys are random/non-semantic and the server revalidates metadata before scanning.
